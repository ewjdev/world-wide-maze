# Build log: Phase 05 (Physics and ball feel)

- **Agent:** Claude Opus 5.5 (1M context), run as a Claude Code sub-agent in an isolated git worktree (branch `worktree-agent-a855d09090ce44034`).
- **Start / end:** 2026-09-25 ~08:06Z → ~08:40Z.
- **Environment:** macOS (Apple M5 Max), Node 26.0.0, pnpm 11.5.0, Rapier 0.20.0, Playwright 1.63 Chromium.

## Instructions received (summary)
- Execute `plans/phase-05-physics.md`. Its "G0 updates" section overrides the earlier text:
  - Tilt is a gravity rotation in the `frameYaw` frame.
  - Use the exact 2013 friction, restitution and damping values.
  - Elevators are trigger-activated.
  - Implement the fall/lost semantics.
- Test against `fixtures/stages/handmade-simple.json` and the gitignored `reference/aid-dcc.stage.json` (the converted 2013 stage).
- Deliver `fixtures/replays/handmade-simple.keyboard.json`, reaching the goal (a minimal waypoint pilot in test code is fine).
- Owned paths: `packages/physics/**`, `fixtures/replays/**`, `apps/web/src/dev/physics-sandbox.tsx`, plus one route line.
- Don't depend on `@wwm/engine`. Evaluate the compat vs deterministic Rapier builds. Verify the Rapier API from the installed typings.

## What was built
- `@wwm/physics`:
  - `RapierSimulation` (`createSimulation`) implements `Simulation`.
  - `createWorkerSimulation` runs it in a Web Worker with its own 120 Hz clock, a stall guard, latched jump edges and snapshot interpolation.
  - `replay` / `runInputs` / `record`.
  - Pure StageData → collider geometry: an earcut prism trimesh per island (with `FIX_INTERNAL_EDGES`), bridge decks with flat aprons into both islands, and rails.
  - `params.ts`: every tunable labelled E/R/N.
  - `dmath.ts`: deterministic sin and cos.
- Fixture: `fixtures/replays/handmade-simple.keyboard.json`, 5,429 samples (45.2 s), generated by `pnpm --filter @wwm/physics replay:make`. The route crosses the start island (2 small items), a flat bridge, a large item, the ramp, 4 small items, the elevator up, 3 more small items and a second large item, then reaches the goal.
- `/dev/physics` sandbox:
  - Canvas-2D top-down draw of the Rapier collider wireframe, plus items, goal, elevators (shaded by height), a ball trail and the frameYaw arrow.
  - 2013 keyboard mapping (±25° ramped at 162°/s, any arrow = POWER with a 100 ms release, Space, R).
  - Worker/main-thread switch, 60 Hz parity switch, and sliders for every numeric param with JSON export.
  - Feel checklist, event log, a stage picker (including `reference/*.stage.json` through `import.meta.glob`) and JSON upload.
  - `?selftest=1` for Playwright.
- The web app gained `@wwm/physics` as a dependency (needed so Vite and tsc resolve the sandbox import) and one lazy route line.

## Attempts that failed, and why
- **The first fall test didn't fall.** With POWER released, the evidenced 4.61/s angular damping brakes a 12 m/s ball in about 5.5 m. The fix was the test, not the model: the throw now starts closer to the edge.
- **The first rail-bounce test saw no rebound.** The same braking applied, plus top-spin carrying the ball back into the rail. The test now holds POWER (active damping). "Not trapped" is checked by tilting away afterwards.
- **The jump-in-mid-air test** teleported the ball 2 ticks after a contact, which is inside the evidenced 100 ms grace. I corrected the test.
- **aid-dcc traversal, attempt 1:** a lead-out waypoint sat behind a rail on a 16 px strip, so the pilot pushed into the rail forever. I added stall-skipping to the test pilot.
- **aid-dcc attempt 2 found a real physics bug.**
  - Island 20 is a 14 px (1.04 m) strip. My rails were 0.1 m boxes centred on the edge, which left 0.94 m for a 1 m ball, so the ball wedged between the rails.
  - 2013 rails were zero-thickness ribbons at the edge.
  - Fix: rail boxes now sit just **outside** the edge line (islands, bridge sides and elevator platforms).
  - Then I regenerated the fixture replay. This is why replays are tied to `PHYSICS_VERSION`.
- **aid-dcc attempt 3:** straight lines between mouths cut across C-shaped text-blob islands. I added a small per-island grid BFS to the test route helper. The Phase 09 solver owns real routing.
- **Negative-control tunneling test:** the first deck-drop detector only counted balls still inside a 1 m band. I replaced it with a crossing-point test (the position where the ball passes the deck's bottom plane).

## Manual human interventions
None.

## Test evidence (`pnpm check` green: 24 files, 172 tests; physics has 43 tests)
- **Determinism:**
  - The fixture replay run 3× gives identical event streams, and the final `BallState` is bit-identical (asserted within 1e-6 and with `toEqual`).
  - Reusing one `Simulation` across `load()`s gives the same result.
  - **The replay in a Chromium Web Worker equals the Node result bit-for-bit** (Playwright, `/dev/physics?selftest=1`).
  - aid-dcc: a 23,281-tick run replays identically.
- **Goal:** the fixture reaches `goal` on its last tick, with islands 0→1→2→3, 1 elevator ride, 11 distinct items (2 large), and no falls.
- **Fall/restart:** `fell` gives `restartAt` = the nearest restart point on the last-touched island. `lost` comes exactly 360 ticks later. After `reset()` the ball is grounded at the point. While falling, input is ignored and gravity is ×2 (measured > 85 m/s² after damping).
- **Items:** each fires once, even when the ball sits on it. The goal fires once.
- **Tunneling:** 0 / 1,000 randomized trials, at up to 2× the measured max rolling speed (19.2 m/s → 38.4 m/s):
  - 500 into a guardrail,
  - 250 into a bridge side rail,
  - 250 dropped onto a bridge deck edge at up to 80 m/s.
  - Negative control: with the rails and deck removed, the same detectors fire in >15/20 trials.
- **Feel (vs `FEEL_CHECKS`):**
  - Jump apex 2.32 m (target 2.3 ± 0.35).
  - Downhill acceleration at 45° within ±3 of 23.4 m/s².
  - Can't reach the top of a +3 m island.
  - Tilt direction is correct for 4 yaw values.
  - No POWER means tilt is ignored.
  - POWER release brakes: stops in < 6 m from 8 m/s, and < 0.8× the POWER-held coast distance.
- **Perf** (Node, M5 Max; logged by `test/perf.test.ts`):
  - A synthetic 412,704-triangle stage (25,946 colliders) loads in ~150 ms. The first step is ~28 ms (BVH build). After that the mean is 0.035 ms per step, p95 0.083 ms and max ~1 ms, with the ball grounded 69 % of ticks.
  - aid-dcc (15,892 tris, 1,599 colliders): mean 0.015 ms per step.
  - Worker in Chromium: 120–121 steps/s while rendering the sandbox.
- **aid-dcc traversal:** start → goal in 194 s of sim time through 21 islands, 4 elevator rides (up and down), 49 items, and no falls.
- **Rapier builds** (`bench:builds`): deterministic 21.5 / 18.5 µs per step vs standard 16.5 / 17.8. The deterministic build was chosen.
- **Screenshots** (gitignored): `test-results/phase-05/sandbox.png` (handmade) and `test-results/phase-05/aid.png` (aid-dcc in the sandbox).

## Remaining defects / follow-ups
- The worker `step()` is non-blocking and follows wall time, so worker play is not tick-deterministic by design. Deterministic runs use `createSimulation` or `replayInWorker`. There's no SharedArrayBuffer path: postMessage snapshots are about 0.3 KB each, and COOP/COEP isn't configured.
- Rolling contact shows a small vertical velocity jitter (±0.1 m/s) from Rapier's contact solver. It's invisible in position (< 1 mm), and `grounded` is stable because of the 2 cm touch tolerance.
- The feel values are evidenced constants, but they haven't been playtested with a phone yet (Phase 06/08).
- `reset()` to a point off every island falls back to the nearest island through `distanceToPolygonEdge` (which uses `Math.hypot`). That's only relevant for a malformed `restartAt`.

---

# Phase 05b: physics 0.2.0 (low-rise lifts, precompiled WASM)

- **Agent / date / environment:** as Phase 03b (`phase-03.md`), same session and branch.

## What changed (`PHYSICS_VERSION` 0.1.0 → **0.2.0**)
- **BI-3, the lift wedge.** Both platforms share one footprint. After a ride down, the partner platform came back
  right above the ball; with a rise under ball diameter + slab (1.463 D) its colliders were re-enabled around the
  ball, which was pushed into the lower slab and couldn't move (a softlock). Now:
  - the partner's colliders stay disabled (`pending`) until the ball has left its volume (footprint grown by the ball
    radius + rail, slab to rail top), checked every step;
  - on a ride down with a rise under 1.463 D + 5 cm, the ball is eased along the platform axis (same cubic easing as
    the platform) to just clear the upper island's edge. Without this, a ball that boarded at the platform's upper
    end sat partly under the upper island's slab and was wedged there instead.
  - Rides with a normal rise (every lift the builder makes now: ≥ 3.7 D) are unchanged.
- **`loadRapier({ wasmModule })`** (additive): initialise Rapier from a precompiled `WebAssembly.Module` of the
  package's `dist/rapier_wasm3d_bg.wasm`, for runtimes that forbid WASM codegen (workerd). The compat build's `init()`
  takes no arguments, so while it initialises, its single `WebAssembly.instantiate(bytes)` call is answered with the
  module (scoped, restored in `finally`; it throws if the module wasn't used). A failed load is no longer cached.
  `loadRapier('deterministic')` still works. The Worker's replay verification and playability validation both use it.
- Fixture replay regenerated (`pnpm --filter @wwm/physics replay:make`): byte-identical inputs, same events, goal at
  tick 5429. Replays stored with 0.1.0 are now verified as "stale version" (by design).

## Test evidence
- `test/simulation.test.ts`: lifts rising 0.5, 1.04 and 6 D: ride down, then the ball rolls ≥ 30 px off the lower
  end (before: 1.04 D stayed pinned with |v| = 0).
- `test/rapier.test.ts`: the shipped `.wasm` is byte-identical (sha256) to the inlined copy; with
  `WebAssembly.instantiate(bytes)` made to throw like workerd, `loadRapier()` fails and isn't cached, then
  `loadRapier({ wasmModule })` initialises and a simulation runs.
- Whole package: 47 passed, 2 skipped (reference/browser).
- In workerd: `apps/worker/test/solver.workerd.test.ts` (see Phase 03b).

## Attempts that failed
- The partner-platform deferral alone didn't free the ball at 1.04 D (the upper island's slab wedged it); the
  along-axis easing was added after the new test exposed it.
