# @wwm/net

Controller transport: room client, tilt pipeline and filtering on top of the @wwm/schema codec.

**Status:** scaffold (Phase 02). Owned and implemented by **Phase 06** — see `plans/phase-06-*.md`.

- Contract types come from `@wwm/schema` (never redeclare them).
- Tests: `pnpm vitest run --project @wwm/net` (or `pnpm test` at the root).
- Typecheck: `pnpm --filter @wwm/net typecheck`.
