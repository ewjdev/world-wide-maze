# @wwm/solver

Solver bot and stage validation (headless playthroughs, seed reroll).

**Status:** scaffold (Phase 02). Owned and implemented by **Phase 09** — see `plans/phase-09-*.md`.

- Contract types come from `@wwm/schema` (never redeclare them).
- Tests: `pnpm vitest run --project @wwm/solver` (or `pnpm test` at the root).
- Typecheck: `pnpm --filter @wwm/solver typecheck`.
