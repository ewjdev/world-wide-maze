# @wwm/engine

three.js world: textured islands, ocean, ball, glow, camera, performance ladder.

**Status:** scaffold (Phase 02). Owned and implemented by **Phase 04** — see `plans/phase-04-*.md`.

- Contract types come from `@wwm/schema` (never redeclare them).
- Tests: `pnpm vitest run --project @wwm/engine` (or `pnpm test` at the root).
- Typecheck: `pnpm --filter @wwm/engine typecheck`.
