# @wwm/ai

Optional AI Remix: semantic theming and route proposals, always behind the validator.

**Status:** scaffold (Phase 02). Owned and implemented by **Phase 11** — see `plans/phase-11-*.md`.

- Contract types come from `@wwm/schema` (never redeclare them).
- Tests: `pnpm vitest run --project @wwm/ai` (or `pnpm test` at the root).
- Typecheck: `pnpm --filter @wwm/ai typecheck`.
